/**
 * Although next will help us to build a 404 page automatically.
 * But as if we create a 404 page by ourself. we can do this by following...
 *
 * 404.jsx or 404/index.jsx in the root catelog
 */

const NotFoundPage = () => {
	return (
		<div>
			<h1>404</h1>
		</div>
	);
};

export default NotFoundPage;
